.wrapper {
    border: solid 4px black;
    margin-bottom: 5px;
}

/* font-family: 'Cinzel', serif;
font-family: 'Great Vibes', cursive;
font-family: 'Quicksand', sans-serif; */

.full-width {
    width: 100%;
    height: 120px;
    background-color: rgb(134, 181, 165);
}
header {
    position: fixed;

}

h1 {
    overflow: hidden;
    font-family: great Vibes, cursive;
    text-align: center;
}

.moon {
    overflow: hidden;
    padding-left: 40%;
    padding-top: 18px;
}
    
nav {
    position: fixed;
}

.navbar a {  
    font-family: cinzel, serif;
    display: block;
    text-align: center;
    color:black
}
   
/* .navbar {
    padding-top: 20px;
} */

a { text-decoration: none;
color: black;}

.home {
    padding-top: 34px;
}
.contact {
    padding-top: 4px;
}
.about {
    padding-top: 4px;
}

/* .audio {
    position: fixed;
    float: right;
} */
    
aside {
    background-color: #8d09a1;
    clear: left;
}

section {
    background-color: antiquewhite;
}

footer {
    margin-top: 5px;
}

.foot {
    height: 20px;
    width: 180px;
    margin: 0 auto;
    text-align: center;
    color: black;
    padding-top: 45px;
    
}
.insta {
    padding-top: 16px;
}

.fb {
    padding-top: 10px;
}

.float-right {
    float:right;
    width: 50%;
    height: 605px;
    margin-top: 5px;
}
.float-left {
    width: 50%;
    height: 605px;
    margin-top: 5px;
}

.video {
    margin-top: 5px;
}

/* Contact form */
/* Style inputs with type="text", select elements and textareas */
input[type=text], select, textarea {
    width: 100%; /* Full width */
    padding: 12px; /* Some padding */ 
    border: 1px solid rgb(238, 196, 235); /* Gray border */
    border-radius: 4px; /* Rounded borders */
    box-sizing: border-box; /* Make sure that padding and width stays in place */
    margin-top: 6px; /* Add a top margin */
    margin-bottom: 16px; /* Bottom margin */
    resize: vertical /* Allow the user to vertically resize the textarea (not horizontally) */
  }
  
  /* Style the submit button with a light purple background color*/
  input[type=submit] {
    background-color: #b94fd6;
    color: white;
    padding: 12px 20px;
    border: none;
    border-radius: 4px;
    /* cursor: pointer; */
  }
  
  /* When moving the mouse over the submit button, add a darker purple color */
  input[type=submit]:hover {
    background-color: #8d09a1;
  }
  
  /* Add a background color and some padding around the form */
  .container {
    border-radius: 5px;
    background-color: #f6ddf5;
    padding: 20px;
  }

  .info {
    height: 600px;
    width: 100%;
    background-color: blanchedalmond;
  }

  .photo {
    height: 400px;
    width: 400px;
    margin: 0 auto;
    background-color: aliceblue;
    }